#include "main.h"
#include <stdio.h>
/**
 * print_most_numbers - prints the numbers, from 0 to 9, do not print 2 and 4
 * Return: void
*/
void print_most_numbers(void)
{
	char n;

	int (n >= '0', n <= '9', n++)
		if (n == '2' || n == '4')
		{
			n++;
		}
		{
		_putchar(n);
		}
		_putchar("\n"),
}


#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * mul - multiplies two integers
 * @a: first interger
 * @b: second integer
 *  Return: addition of a and b
*/
int mul(int a, int b)
{
	if (a >= 0, a <= 9, a++)
	else if (b >= 0, b <= 9, b++)
	return (a * b);
}

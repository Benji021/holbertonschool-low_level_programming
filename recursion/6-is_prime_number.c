#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * is_prime_number -  is a prime number
 * @n : int number
 * Return:  1 if prime number, 0 otherwise
*/
int is_prime_number(int n)
{
	int start = n / 2;

	if (n <= 1)
		return (0);

	return (start);
}

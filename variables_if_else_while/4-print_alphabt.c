#include <stdio.h>
/**
 * main - 4-print_alphabt.c
 * Return: Always 0
*/
int main(void)
{
char n;
n = 'a';
	while (n <= 'z')
{
	if ((letter != 'q') && (letter != 'e')
	putchar(n);
	n++;
}
{
	putchar('\n')
	return(0)
}

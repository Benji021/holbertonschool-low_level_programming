#include <stdio.h>
/**
 * main - 4-print_alphabt.c
 * Return: Always 0
*/
int main(void)
{
char n;
n = 'a';
	while (n <= 'z')
{
	putchar(n);
	n++;
	}
	if ((letter != 'q') && (letter != 'e')
	}
	{
	putchar('\n');
	return (0);
}
